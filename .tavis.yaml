sudo: required
language: generic 
services:
    - docker

before_install:
    - docker build -t hasanagh/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true hasanagh/docker-react npm run test